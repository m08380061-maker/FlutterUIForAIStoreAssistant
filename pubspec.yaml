name: ai_store_assistant
description: AI-powered retail management platform for grocery stores and small businesses.
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # Navigation
  go_router: ^14.2.0

  # State Management
  provider: ^6.1.2

  # Storage
  shared_preferences: ^2.2.3
  flutter_secure_storage: ^9.0.0

  # UI
  google_fonts: ^6.2.1
  fl_chart: ^0.68.0

  # Internationalization
  intl: ^0.20.2

  # Icons
  cupertino_icons: ^1.0.6
  drift: ^2.19.0
  sqlite3_flutter_libs: ^0.5.4
  path_provider: ^2.1.4
  path: ^1.9.1
  uuid: ^4.6.0

  # Local AI — offline inference (no cloud APIs)
  ffi: ^2.1.0             # dart:ffi utilities (StringUtf8Pointer, using(), etc.)
  onnxruntime: ^1.4.1    # ONNX Runtime mobile for on-device vision inference
  image: ^4.2.0           # Pure-Dart image decode/resize/normalise for ONNX input

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  drift_dev: ^2.19.0
  build_runner: ^2.4.0

flutter:
  uses-material-design: true
  generate: true
